const styles = {
  page: { minHeight: '100vh', padding: '40px 0 80px' },
  header: { display: 'flex', justifyContent: 'space-between', alignItems: 'center', marginBottom: 32, flexWrap: 'wrap', gap: 16 },
  title: { fontSize: 36, fontWeight: 700, color: 'var(--foreground, #111827)', margin: 0 },
  refreshButton: { display: 'flex', alignItems: 'center', gap: 8 },
  tabs: { display: 'flex', gap: 12, marginBottom: 32, borderBottom: '2px solid hsl(var(--border))' },
  tab: {
    display: 'flex',
    alignItems: 'center',
    gap: 8,
    padding: '12px 24px',
    background: 'none',
    border: 'none',
    borderBottom: '3px solid transparent',
    cursor: 'pointer'
  },
  orderHeader: { display: 'flex', justifyContent: 'space-between', alignItems: 'flex-start', marginBottom: 16, flexWrap: 'wrap', gap: 16 },
  orderInfo: { flex: 1, minWidth: 300 },
  orderNumber: { fontSize: 20, fontWeight: 700, marginBottom: 8, color: 'hsl(var(--foreground))' },
  orderDate: { fontSize: 14, color: 'hsl(var(--muted-foreground))', marginBottom: 4 },
  customerInfo: { fontSize: 14, color: 'hsl(var(--foreground))', marginTop: 8 },
  statusSection: { display: 'flex', flexDirection: 'column', gap: 8, minWidth: 200 },
  statusLabel: { fontSize: 12, fontWeight: 600, color: 'hsl(var(--muted-foreground))', textTransform: 'uppercase' },
  statusSelect: {
    padding: '10px 14px',
    borderRadius: 8,
    border: '2px solid',
    fontSize: 14,
    fontWeight: 600,
    cursor: 'pointer',
    outline: 'none'
  },
  divider: { height: 1, backgroundColor: 'hsl(var(--border))', margin: '20px 0' },
  orderDetails: { display: 'grid', gridTemplateColumns: '1fr 1fr', gap: 24, marginBottom: 16 },
  section: { marginBottom: 0 },
  sectionTitle: { fontSize: 16, fontWeight: 600, color: 'hsl(var(--foreground))', marginBottom: 12 },
  orderItems: { display: 'flex', flexDirection: 'column', gap: 8 },
  itemRow: { 
    display: 'flex', 
    justifyContent: 'space-between', 
    alignItems: 'center',
    padding: '8px 12px',
    backgroundColor: 'hsl(var(--muted))',
    borderRadius: 6,
    fontSize: 14
  },
  itemName: { flex: 1, color: 'hsl(var(--foreground))', fontWeight: 500 },
  itemQty: { color: 'hsl(var(--muted-foreground))', margin: '0 12px' },
  itemPrice: { fontWeight: 600, color: 'hsl(var(--foreground))', minWidth: 80, textAlign: 'right' },
  addressText: { fontSize: 14, color: 'hsl(var(--foreground))', lineHeight: 1.6, backgroundColor: 'hsl(var(--muted))', padding: 12, borderRadius: 6 },
  orderFooter: { marginTop: 16 },
  totalSection: { 
    display: 'flex', 
    justifyContent: 'space-between', 
    alignItems: 'center',
    padding: '16px',
    backgroundColor: 'hsl(var(--muted))',
    borderRadius: 8
  },
  totalLabel: { fontSize: 18, fontWeight: 600, color: 'hsl(var(--foreground))' },
  totalValue: { fontSize: 24, fontWeight: 700, color: 'hsl(var(--primary))' },
  addButton: { display: 'flex', alignItems: 'center', gap: 8, marginBottom: 24 },
  productsGrid: { display: 'grid', gridTemplateColumns: 'repeat(auto-fill, minmax(250px, 1fr))', gap: 20 },
  productCard: { padding: 16 },
  productImage: { width: '100%', height: 200, objectFit: 'cover', borderRadius: 8, marginBottom: 12 },
  productCardName: { fontSize: 18, fontWeight: 600, marginBottom: 8 },
  productPrice: { fontSize: 16, color: 'hsl(var(--primary))', fontWeight: 600, marginBottom: 12 },
  productActions: { display: 'flex', gap: 8 },
  actionButton: { display: 'flex', alignItems: 'center', gap: 6, flex: 1 },
  tableCard: { padding: 20, overflowX: 'auto' },
  table: { width: '100%', borderCollapse: 'collapse' },
  th: { textAlign: 'left', padding: '12px', borderBottom: '2px solid hsl(var(--border))', fontWeight: 600 },
  td: { padding: '12px', borderBottom: '1px solid hsl(var(--border))' },
  modalTitle: { fontSize: 24, fontWeight: 700, marginBottom: 20 },
  form: { display: 'flex', flexDirection: 'column', gap: 16 },
  label: { fontSize: 14, fontWeight: 600, color: 'hsl(var(--foreground))' },
  input: { padding: 12, borderRadius: 8, border: '2px solid hsl(var(--border))', fontSize: 14, backgroundColor: 'hsl(var(--background))', color: 'hsl(var(--foreground))' },
  row: { display: 'flex', gap: 12 },
  col: { flex: 1 },
  checkboxLabel: { display: 'flex', alignItems: 'center', gap: 8, cursor: 'pointer' },
  checkbox: { width: 18, height: 18 },
  modalActions: { display: 'flex', gap: 12, marginTop: 8 },
  statsGrid: { display: 'grid', gridTemplateColumns: 'repeat(auto-fit, minmax(250px, 1fr))', gap: 20, marginBottom: 32 },
  statCard: { padding: 24, display: 'flex', alignItems: 'center', gap: 16 },
  statIcon: { width: 56, height: 56, borderRadius: 12, display: 'flex', alignItems: 'center', justifyContent: 'center', flexShrink: 0 },
  statContent: { flex: 1 },
  statLabel: { fontSize: 14, color: 'hsl(var(--muted-foreground))', marginBottom: 4 },
  statValue: { fontSize: 28, fontWeight: 700, color: 'hsl(var(--foreground))' },
  chartCard: { padding: 24, marginBottom: 32 },
  chartTitle: { fontSize: 20, fontWeight: 700, marginBottom: 24, color: 'hsl(var(--foreground))' },
  chartContainer: { display: 'flex', alignItems: 'flex-end', gap: 16, height: 250, padding: '20px 0' },
  barContainer: { flex: 1, display: 'flex', flexDirection: 'column', alignItems: 'center' },
  barWrapper: { width: '100%', height: 200, display: 'flex', alignItems: 'flex-end', justifyContent: 'center' },
  bar: { width: '80%', backgroundColor: 'hsl(var(--primary))', borderRadius: '4px 4px 0 0', position: 'relative', minHeight: 4, transition: 'all 0.3s' },
  barValue: { position: 'absolute', top: -24, left: '50%', transform: 'translateX(-50%)', fontSize: 12, fontWeight: 600, color: 'hsl(var(--foreground))', whiteSpace: 'nowrap' },
  barLabel: { fontSize: 12, color: 'hsl(var(--muted-foreground))', marginTop: 8, textAlign: 'center' },
  barOrders: { fontSize: 10, color: 'hsl(var(--muted-foreground))', marginTop: 4 },
  grid: { display: 'grid', gridTemplateColumns: '1fr 1fr', gap: 24, marginBottom: 32 },
  gridCard: { padding: 24 },
  productList: { display: 'flex', flexDirection: 'column', gap: 12 },
  productItem: { display: 'flex', alignItems: 'center', gap: 12, padding: 12, backgroundColor: 'hsl(var(--muted))', borderRadius: 8 },
  productRank: { width: 32, height: 32, borderRadius: '50%', backgroundColor: 'hsl(var(--primary))', color: 'hsl(var(--primary-foreground))', display: 'flex', alignItems: 'center', justifyContent: 'center', fontWeight: 700, fontSize: 14, flexShrink: 0 },
  productInfo: { flex: 1 },
  productName: { fontSize: 16, fontWeight: 600, color: 'hsl(var(--foreground))', marginBottom: 4 },
  productStats: { fontSize: 12, color: 'hsl(var(--muted-foreground))' },
  usersList: { display: 'flex', flexDirection: 'column', gap: 12, maxHeight: 400, overflowY: 'auto' },
  userItem: { display: 'flex', justifyContent: 'space-between', alignItems: 'center', padding: 12, backgroundColor: 'hsl(var(--muted))', borderRadius: 8 },
  userInfo: { flex: 1 },
  userName: { fontSize: 14, fontWeight: 600, color: 'hsl(var(--foreground))', marginBottom: 4 },
  userEmail: { fontSize: 12, color: 'hsl(var(--muted-foreground))' },
  userDate: { fontSize: 12, color: 'hsl(var(--muted-foreground))' },
  recentOrdersCard: { padding: 24 },
  recentOrdersList: { display: 'flex', flexDirection: 'column', gap: 12 },
  recentOrderItem: { display: 'flex', justifyContent: 'space-between', alignItems: 'center', padding: 16, backgroundColor: 'hsl(var(--muted))', borderRadius: 8 },
  recentOrderNumber: { fontSize: 14, fontWeight: 600, color: 'hsl(var(--foreground))', marginBottom: 4 },
  recentOrderCustomer: { fontSize: 12, color: 'hsl(var(--muted-foreground))' },
  recentOrderAmount: { fontSize: 16, fontWeight: 700, color: 'hsl(var(--primary))', marginRight: 16 },
  recentOrderStatus: { fontSize: 12, fontWeight: 600, textTransform: 'capitalize', padding: '4px 12px', borderRadius: 12, backgroundColor: 'hsl(var(--muted))' },
  emptyText: { textAlign: 'center', color: 'hsl(var(--muted-foreground))', padding: 20 },
  accessDenied: {
    maxWidth: 500,
    margin: '100px auto',
    padding: 40,
    textAlign: 'center'
  },
  accessDeniedTitle: {
    fontSize: 28,
    fontWeight: 700,
    color: 'hsl(var(--destructive))',
    marginBottom: 16
  },
  accessDeniedText: {
    fontSize: 16,
    color: 'hsl(var(--muted-foreground))',
    marginBottom: 24,
    lineHeight: 1.6
  },
  backButton: {
    padding: '12px 24px',
    fontSize: 16
  }
};

export default AdminPanel;
